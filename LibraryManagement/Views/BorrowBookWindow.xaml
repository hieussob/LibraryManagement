<Window x:Class="LibraryManagement.Views.BorrowBookWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LibraryManagement.Views"
        mc:Ignorable="d"
        Title="Tạo phiếu mượn sách" Height="700" Width="700"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Thông tin người mượn -->
        <GroupBox Grid.Row="0" Header="Thông tin người mượn" Padding="15" Margin="0,0,0,15" BorderBrush="#DDD">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Row 0: Tên và Cấp Bậc -->
                <TextBlock Grid.Row="0" Grid.Column="0" Text="Họ tên *:" VerticalAlignment="Center" Margin="0,0,10,10" FontWeight="SemiBold"/>
                <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding BorrowRecord.BorrowerName, UpdateSourceTrigger=PropertyChanged}" 
                         Padding="8" FontSize="14" Margin="0,0,0,10" BorderBrush="#CCC" BorderThickness="1"/>

                <TextBlock Grid.Row="0" Grid.Column="3" Text="Cấp bậc:" VerticalAlignment="Center" Margin="0,0,10,10" FontWeight="SemiBold"/>
                <TextBox Grid.Row="0" Grid.Column="4" Text="{Binding BorrowRecord.Rank, UpdateSourceTrigger=PropertyChanged}" 
                         Padding="8" FontSize="14" Margin="0,0,0,10" BorderBrush="#CCC" BorderThickness="1"/>

                <!-- Row 1: Số điện thoại và Chức Vụ -->
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Số điện thoại *:" VerticalAlignment="Center" Margin="0,0,10,10" FontWeight="SemiBold"/>
                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding BorrowRecord.BorrowerPhone, UpdateSourceTrigger=PropertyChanged}" 
                         Padding="8" FontSize="14" Margin="0,0,0,10" BorderBrush="#CCC" BorderThickness="1"/>

                <TextBlock Grid.Row="1" Grid.Column="3" Text="Chức vụ:" VerticalAlignment="Center" Margin="0,0,10,10" FontWeight="SemiBold"/>
                <TextBox Grid.Row="1" Grid.Column="4" Text="{Binding BorrowRecord.Position, UpdateSourceTrigger=PropertyChanged}" 
                         Padding="8" FontSize="14" Margin="0,0,0,10" BorderBrush="#CCC" BorderThickness="1"/>

                <!-- Row 2: Số điện thoại và Đơn Vị -->
                <TextBlock Grid.Row="2" Grid.Column="0" Text="Số điện thoại:" VerticalAlignment="Center" Margin="0,0,10,10" FontWeight="SemiBold"/>
                <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding BorrowRecord.BorrowerPhone, UpdateSourceTrigger=PropertyChanged}" 
                         Padding="8" FontSize="14" Margin="0,0,0,10" BorderBrush="#CCC" BorderThickness="1"/>

                <TextBlock Grid.Row="2" Grid.Column="3" Text="Đơn vị:" VerticalAlignment="Center" Margin="0,0,10,10" FontWeight="SemiBold"/>
                <TextBox Grid.Row="2" Grid.Column="4" Text="{Binding BorrowRecord.Unit, UpdateSourceTrigger=PropertyChanged}" 
                         Padding="8" FontSize="14" Margin="0,0,0,10" BorderBrush="#CCC" BorderThickness="1"/>
            </Grid>
        </GroupBox>

        <!-- Thông tin mượn -->
        <GroupBox Grid.Row="1" Header="Thông tin mượn" Padding="15" Margin="0,0,0,15" BorderBrush="#DDD">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Ngày mượn:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <DatePicker SelectedDate="{Binding BorrowRecord.BorrowDate}" 
                                Padding="8" FontSize="14" BorderBrush="#CCC" BorderThickness="1"/>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="Ngày hẹn trả:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <DatePicker SelectedDate="{Binding BorrowRecord.DueDate}" 
                                Padding="8" FontSize="14" BorderBrush="#CCC" BorderThickness="1"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <!-- Danh sách sách mượn -->
        <Border Grid.Row="2" BorderBrush="#DDD" BorderThickness="1" CornerRadius="3" Background="White" Margin="0,0,0,15">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" Text="Danh sách sách mượn" 
                           FontWeight="SemiBold" FontSize="14" 
                           Background="#F5F5F5" Padding="0"/>
                <DataGrid ItemsSource="{Binding BorrowedBooks, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                          AutoGenerateColumns="False" 
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          SelectionMode="Single"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          RowHeight="35"
                          BorderThickness="0"
                          Background="White"
                          AlternatingRowBackground="#F9F9F9">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Tên sách" Binding="{Binding BookTitle, Mode=OneWay}" Width="*" MinWidth="200" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Tác giả" Binding="{Binding Author, Mode=OneWay}" Width="150" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Số lượng" Binding="{Binding Quantity, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="100"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>

        <!-- Ghi chú và buttons -->
        <Grid Grid.Row="3" Margin="0,15,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,15">
                <TextBlock Text="Ghi chú:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <TextBox Text="{Binding BorrowRecord.Notes, UpdateSourceTrigger=PropertyChanged}"
                         TextWrapping="Wrap" AcceptsReturn="True"
                         Height="60" Padding="8" FontSize="14" 
                         BorderBrush="#CCC" BorderThickness="1" VerticalScrollBarVisibility="Auto"/>
            </StackPanel>

            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="💾 Lưu phiếu mượn" Command="{Binding SaveCommand}" 
                        CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                        Padding="20,10" Margin="0,0,10,0" FontSize="14" 
                        Background="#4CAF50" Foreground="White" BorderThickness="0" Cursor="Hand"/>
                <Button Content="❌ Hủy" IsCancel="True" 
                        Padding="20,10" FontSize="14" 
                        Background="#9E9E9E" Foreground="White" BorderThickness="0" Cursor="Hand"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
