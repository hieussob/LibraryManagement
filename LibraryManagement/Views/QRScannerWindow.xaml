<Window x:Class="LibraryManagement.Views.QRScannerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LibraryManagement.Views"
        xmlns:converters="clr-namespace:LibraryManagement.Converters"
        mc:Ignorable="d"
        Title="Quét mã QR" Height="650" Width="650"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5"
        Closing="Window_Closing">
    
    <Window.Resources>
        <converters:InverseBoolConverter x:Key="InverseBoolConverter"/>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Instructions -->
        <Border Grid.Row="0" Background="#E3F2FD" BorderBrush="#2196F3" BorderThickness="1" CornerRadius="5" Padding="15" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="💡 Hướng dẫn quét QR" FontWeight="Bold" FontSize="14" Foreground="#1976D2" Margin="0,0,0,5"/>
                <TextBlock TextWrapping="Wrap" FontSize="12" Foreground="#424242">
                    <Run Text="• Có camera: Chọn camera và nhấn 'Bắt đầu quét'"/>
                    <LineBreak/>
                    <Run Text="• Không có camera: Dùng điện thoại quét QR, nhập mã sách vào ô bên dưới"/>
                </TextBlock>
            </StackPanel>
        </Border>

        <!-- Camera Selection -->
        <GroupBox Grid.Row="1" Header="Quét bằng Camera (nếu có)" Padding="10" Margin="0,0,0,15" BorderBrush="#DDD">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <ComboBox Grid.Column="0" ItemsSource="{Binding AvailableCameras}" 
                          SelectedItem="{Binding SelectedCamera}"
                          FontSize="14" Padding="8" BorderBrush="#CCC" BorderThickness="1"/>
                <Button Grid.Column="2" Content="▶ Bắt đầu quét" Command="{Binding StartScanningCommand}"
                        IsEnabled="{Binding IsScanning, Converter={StaticResource InverseBoolConverter}}"
                        Padding="15,8" FontSize="14" Background="#4CAF50" Foreground="White" BorderThickness="0" Cursor="Hand"/>
            </Grid>
        </GroupBox>

        <!-- Camera Preview -->
        <Border Grid.Row="2" BorderBrush="#DDD" BorderThickness="2" Background="Black" Margin="0,0,0,15">
            <Image x:Name="CameraPreview" Stretch="Uniform"/>
        </Border>

        <!-- Scanned Result -->
        <GroupBox Grid.Row="3" Header="Kết quả quét (hoặc nhập thủ công)" Padding="10" Margin="0,0,0,15" BorderBrush="#DDD">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBlock Text="📱 Dùng điện thoại: " FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBlock Text="Quét QR bằng điện thoại, nhập mã sách vào ô bên dưới" 
                               Foreground="#666" VerticalAlignment="Center" FontSize="12"/>
                </StackPanel>
                
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0" Text="Mã sách:" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBox Grid.Column="1" Text="{Binding ScannedText, UpdateSourceTrigger=PropertyChanged}" 
                             FontSize="14" Padding="8" 
                             BorderBrush="#CCC" BorderThickness="1" Background="White"/>
                </Grid>
            </Grid>
        </GroupBox>

        <!-- Debug Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,0,0,10">
            <Button Content="📸 Capture" Click="CaptureFrameButton_Click"
                    IsEnabled="{Binding IsScanning}"
                    Padding="12,8" Margin="0,0,10,0" FontSize="13" 
                    Background="#4CAF50" Foreground="White" BorderThickness="0" Cursor="Hand"
                    ToolTip="Lưu frame hiện tại để debug"/>
            <Button Content="📁 Test File" Click="TestFromFileButton_Click"
                    Padding="12,8" Margin="0,0,10,0" FontSize="13" 
                    Background="#9C27B0" Foreground="White" BorderThickness="0" Cursor="Hand"
                    ToolTip="Chọn ảnh QR code từ máy tính để test decode"/>
            <Button Content="⏹ Dừng" Command="{Binding StopScanningCommand}"
                    IsEnabled="{Binding IsScanning}"
                    Padding="12,8" FontSize="13" 
                    Background="#FF9800" Foreground="White" BorderThickness="0" Cursor="Hand"/>
        </StackPanel>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="✅ OK" Click="OkButton_Click"
                    Padding="25,10" Margin="0,0,10,0" FontSize="14" FontWeight="SemiBold"
                    Background="#2196F3" Foreground="White" BorderThickness="0" Cursor="Hand"/>
            <Button Content="❌ Hủy" IsCancel="True" 
                    Padding="25,10" FontSize="14" FontWeight="SemiBold"
                    Background="#9E9E9E" Foreground="White" BorderThickness="0" Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>
